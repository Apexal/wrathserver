from api.removeBackground import remove_background_b64
from api.utils import b64_to_bytes, b64_to_image


def test_bg_removal():
    # img = b64_to_bytes(b64_png)
    # removed_bg = remove_background(img)
    # with open("out.png", "wb") as f:
    #     f.write(removed_bg)
    pass
